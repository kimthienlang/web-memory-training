import React from "react";

export default function EditProfile() {
  return (
    <button id="edit-profile" className="up-element" onClick={() => alert('this function have not updated!')}>Edit Profile</button>
  )
}
